{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the LocalHub main window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
