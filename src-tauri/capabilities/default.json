{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the LocalHub main window — gated on the production PWA origin so the webview loaded from https://app.containedevolution.com/ can call the listed Tauri APIs (event.listen for the OAuth deep-link callback; opener.openUrl to launch the system browser).",
  "windows": ["main"],
  "remote": {
    "urls": [
      "https://app.containedevolution.com/*",
      "https://www.containedevolution.com/*"
    ]
  },
  "permissions": [
    "core:default",
    "core:event:default",
    "deep-link:default",
    "opener:default",
    "opener:allow-open-url"
  ]
}
