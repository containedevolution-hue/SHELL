{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "The packaged Shell desktop frame. File operations also validate the exact native caller and selected local roots in Rust.",
  "windows": ["desktop"],
  "permissions": ["core:default"]
}
